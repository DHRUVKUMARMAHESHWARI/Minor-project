<!-- Sidebar -->
<aside class="bg-gray-900 overflow-y-hidden flex flex-col px-4 py-6 md:px-10 md:py-10 items-start justify-top gap-4 md:gap-10 w-full md:w-64 text-white space-y-6">

  <!-- Dashboard and Hamburger for Small Screens -->
  <div class="flex justify-between w-full md:hidden">
    <div class="text-xl font-bold">Dashboard</div>
    <button id="menuToggle" class="text-white focus:outline-none">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </div>

  <!-- Full Navigation Menu -->
  <nav id="menu" class="space-y-2 md:space-y-3 w-full hidden md:block">
    <a href="/" class="flex items-center space-x-3 text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-4 rounded-md">
      <span class="text-lg">🏠</span>
      <span>Home</span>
    </a>
    <a href="/dataVisualize" class="flex items-center space-x-3 text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-4 rounded-md">
      <span class="text-lg">📊</span>
      <span>Dashboard</span>
    </a>
    <a href="/profile" class="flex items-center space-x-3 text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-4 rounded-md">
      <span class="text-lg">👤</span>
      <span>Profile</span>
    </a>
    <a href="#" class="flex items-center space-x-3 text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-4 rounded-md">
      <span class="text-lg">🔔</span>
      <span>Notifications</span>
    </a>

    <!-- Logout Button in Navigation -->
    <div class="w-full mt-4">
      <a href="/logout" class="flex items-center space-x-3 text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md">
        <span class="text-lg">🚪</span>
        <span>Logout</span>
      </a>
    </div>
  </nav>
</aside>

<script>
  // JavaScript to toggle the navigation menu visibility on small screens
  const menuToggle = document.getElementById('menuToggle');
  const menu = document.getElementById('menu');

  menuToggle.addEventListener('click', () => {
    menu.classList.toggle('hidden');
  });
</script>